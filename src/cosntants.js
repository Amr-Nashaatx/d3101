export const SVG_HEIGHT = 800;
export const SVG_WIDTH = 800;
export const MARGIN = { top: 20, right: 20, bottom: 40, left: 50 };

export const INNER_WIDTH = SVG_WIDTH - MARGIN.left - MARGIN.right;
export const INNER_HEIGHT = SVG_HEIGHT - MARGIN.top - MARGIN.bottom;
